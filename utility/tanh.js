/**
 * Returns the hyperbolic tangent of the input value.
 *
 * @param {number} value - Input value
 * @returns {number} Output value
 */
export default function tanh(value) {
  return Math.tanh(value);
}